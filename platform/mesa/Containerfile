ARG MESA_IMAGE=ghcr.io/containerpak/mesa-multilib:main
FROM ${MESA_IMAGE}

ARG DEBIAN_FRONTEND=noninteractive

RUN apt-get update && \
    apt-get install -y \
        mesa-utils \
        mesa-utils-extra \
        vulkan-tools && \
    cpak-clean-junk
