FROM ghcr.io/containerpak/mesa-multilib:main

ARG DEBIAN_FRONTEND=noninteractive

RUN apt-get update && \
    apt-get install -y \
        mesa-utils \
        mesa-utils-extra \
        vulkan-tools && \
    cpak-clean-junk
